package options;

// this is so unfinished god

class TheSpecialState extends MusicBeatState
{
    // a random video plays and then the game crashes idk

    /* 
    override public function create()
    {
        // video plays then sys exit bruh
        Sys.exit(0);
    }
    */
}