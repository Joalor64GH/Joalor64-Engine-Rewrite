package minigames;

import minigames.FallingIcon;

// will finish when i have any idea of what im doing
class KillBF extends MusicBeatState
{
    var score:Int;
    var health:Float;

    var beef:FlxTypedGroup<FallingIcon>;

    override function create()
    {
        super.create();
    }

    override function update(elapsed:Float)
    {
        supoer.update(elapsed);
    }
}